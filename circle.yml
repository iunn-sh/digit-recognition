machine:
  python:
    version: 2.7.11

dependencies:
  pre:
    - pip install numpy
    - pip install scipy
    - pip install scikit-learn
    - pip install plotly

test:
  override:
    - python extractor.py
