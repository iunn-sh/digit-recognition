machine:
  python:
    version: 2.7

dependencies:
  pre:
    - pip install numpy
    - pip install scipy
    - pip install scikit-learn
    - pip install plotly

test:
  override:
    - python recognizer.py
